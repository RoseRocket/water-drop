/* global it, describe, before, after */
/* eslint import/no-extraneous-dependencies: ["error", {"devDependencies": true}] */

import { expect } from 'chai';

describe('generate tests', () => {
    it('NEED TO WRITE TESTS', () => {
        expect(true).to.equal(true);
    });
});
